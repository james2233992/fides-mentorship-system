# Copia este contenido en el RAW Editor de Railway Variables

# La DATABASE_URL se genera automáticamente cuando agregas PostgreSQL
# DATABASE_URL=postgresql://...

# Autenticación (IMPORTANTE: Cambia este valor)
JWT_SECRET=tu-clave-secreta-super-segura-cambiar-esto
JWT_EXPIRES_IN=7d

# URLs
FRONTEND_URL=https://fides-frontend.vercel.app
BACKEND_URL=${{RAILWAY_PUBLIC_DOMAIN}}

# Puerto (Railway lo configura automáticamente)
PORT=3000

# Entorno
NODE_ENV=production

# Email (Opcional - Solo si tienes SendGrid)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxx
# EMAIL_FROM=noreply@tudominio.com

# Redis (Solo si lo necesitas)
# REDIS_URL=redis://...

# Prisma
DATABASE_DIRECT_URL=${{DATABASE_URL}}